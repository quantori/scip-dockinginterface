from setuptools import setup, find_packages

setup(name='Docking_Interface',
      version='0.2.4',
      install_requires=[],
      packages=['DockingInterface'],
      include_package_data=True,
)	
